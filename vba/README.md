# VBA配置場所

ここにGitHub上で管理するVBAコードを `.bas` / `.cls` / `.frm` として置いてください。

例：
```text
/vba/一括入力.bas
/vba/フォルダ作成VBA.bas
/vba/個別引き取りVBA.bas
```

`_raw_vbaProject_bin` はExcelから抽出したVBAプロジェクトのバイナリ退避です。CodexやClaudeに読ませる主ファイルではありません。
